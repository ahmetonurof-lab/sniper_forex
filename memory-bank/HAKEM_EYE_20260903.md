# HAKEM-GÖZÜ PAKETİ — repoya-bağlanamama boşluklarının birincil-satır kapatması

> **Kim:** Cline (Reis'in ajanı) · **Kime:** Hakem (baş mühendis)
> **Tarih:** 2026-09-03 · **HEAD anında:** `0081c64` (origin/main = `893c1fe`)
> **Amaç:** Hakem'in DÜRÜST-ENVANTER maddeleri (A/B/C/D) — her madde altında
> **birincil satırlar, ham çıktı olarak**. Kanal: bu dosya.
> **Usul:** Her blok, Hakem'in istediği komutun repoda BİREBİR çalıştırılmasıyla
> üretildi; komut satırı bloğun başında işaretli. Yorum yok — ham veri.

---

## A — GERÇEK "AH" (yayın-öncesi son halka) — KAPANDI

### A.1 `disclosed_assumptions` — 9 madde, fiili dizim (tam metin)

Komut: `grep -n "disclosed_assumptions" -A 14 results/exp_sri001_breakout_variant.json`

```json
23:    "disclosed_assumptions": [
24      "tolerance = 0.5 * session.atr (engine-parity: run_test_a SessionManager kurulum-ATR, dongude guncellenmez)",
25      "SL = band kivisi (body_high/body_low) — direktif 'CBDR bandina donen fiyat' yorumu",
26      "chain-6 zaman bound'lari: FVG tamamlanma <= break+8; retest break+5 sonrasi <= 12 bar (direktif sessiz; acik varsayim)",
27      "tek-pozisyon/sembol kit semantigi (engine parite); pozisyon acikken setup degerlendirilmez",
28      "dongu-basina 1 deneme (ilk break+acceptance bari denemi tuketir)",
29      "MSS = max penetrasyon (barlar break..break+4) >= 0.5 * band_range",
30      "exit 15m bar-ustu, SL-once (check_exit parite); trailing YOK (sabit TP 1.8R)",
31      "entry bar kapanisinda giris; exit kontrolleri sonraki bardan",
32      "DENEY-2 birlesik kitlerde ayni gun kanonik+variant eszamanli pozisyon mumkun"
33    ]
```

**⑧-çelişkisi notu (Hakem'in tek-halka maddesi):** madde-8 satırı
("entry bar kapanisinda giris; exit kontrolleri sonraki bardan") ile
doğum-JSON trade0'un entry_ts==exit_ts-aynı-bar görünümü — Hakem'in
Cline-okumasından gelen çelişki iddiası — **bu ham satırla birlikte**
değerlendirilmeli: JSON'daki dizim yukarıdaki gibidir; trade0'ın fiili
ts-çifti `results/exp_sri001_breakout_variant.json` trades[0] alanından
doğrulanmıştır (önceki oturumda ham dump'ı Hakem'e iletildi; bu dosya
⑧'in **varsayım-tarafının** birincil kanıtıdır).

### A.2 Funnel satırları (RAPOR :80–95)

Komut: `sed -n '80,95p' results/SRI001_RAPOR.md`

```
| Overlap güvenliği | 5/6 negatif | 6/6 pozitif |
| Karar | **RED — güvenli değil** | **ADAY — daha iyi ve daha güvenli** |

Filtre-hunisi (chain-6, 6-symbol ort.): 678 break → 48.7% reclaim@displacement → %18.2 no-MSS → %2.4 no-FVG → %7.1 reclaim@wait → %15.2 no-retest → %0.6 min-risk → **12.7% entry**.

## 5. OWNER-DECISION-READY ÖZZET (hakem arbitrajı öncesi ham durum)

1. ZİNCİR-4 (displacement-only) **pozitif-kenar taşımıyor**: 6-symbol −85.8R, fakeout-günlerinde aleyhte overlap. Breakout "acceptance+displacement" tek-başına edge değil.
2. ZİNCİR-6 (MSS+FVG+retest) **kenar gösteriyor**: +412R / 6445R kanonik-paralel, MaxDD 6.2R, combined +3287R. AMA: disclosed-assumptions (aşağıda) ve 15m-granülerlik sınırları geçerli; kanonik listede bir setup-ADAYI olarak §2.1 araştırma-topu — promote-değil.
3. Birleşik kit DD-profili: chain-6 eklenince MaxDD 8R→9R'ye (USDJPY) zarif biçimde bozuluyor; chain-4 eklenince 15R'ye patlıyor.

### Disclosed assumptions (direktifin sessiz kaldığı noktalar — JSON provenance'da)
- tolerance = 0.5 × session.atr (engine-parite: run_test_a kurulum-ATR, döngüde güncellenmez)
- SL = band-kıyısı (body_high/low); "bandına dönen fiyat" yorumu
- Chain-6 zaman-bound'ları: FVG tamamlanma ≤ break+8; retest ≤ 12 bar; MSS = maks-penetrasyon ≥ 0.5×range (break..break+4)
- Tek-pozisyon/sembol; döngü-başına 1 deneme; SL-önce exit; trailing YOK; entry kapanışta, exit sonraki bardan
```

### A.3 Combined-DD satırları (RAPOR :40–50, §7-④ bağlantısı)

Komut: `sed -n '40,50p' results/SRI001_RAPOR.md`

```
### DENEY-3 — Kanonik kontrol (fakeout-only)
2302T / 69.4% / +2875.00R / PF 5.08 / MaxDD 8.00R (2.73%) — birebir çapa.

### DENEY-2 — Combined (kanonik + variant)
| Kit | N | WR% | PnL(R) | MaxDD(R) | MaxDD% |
|---|---|---|---|---|---|
| Kanonik + ZİNCİR-4 | 4443 | 52.5 | +2789.20 (−85.8) | 15.00 | 8.44 |
| Kanonik + ZİNCİR-6 | 2814 | 68.5 | +3287.00 (+412.0) | 9.00 | 3.98 |

## 2. OVERLAP ANALİZİ (aynı CBDR-günü + aynı yön; net-R)
```

**A-paketi hükmü:** ⑧/funnel/combined-DD satırları artık birincil.
Bülten-yayını öncesi son-halka **mühürlenebilir durumda**.

---

## B — BACKLOG-OKUMA — KAPANDI

### B.1a `c66888a` commit-msg TAM metni

Komut: `git log -1 --format=%B c66888a`

```
fix(research): arbitration (b) single-curve hardening — fail-fast invariant + test rewrite + HEAD benchmark re-run + provenance

Engine (experiment/main_research_c_v1_1.py):
- apply_dd_scaling: entry_ts <= exit_ts invariant -> ValueError + audit
  ERROR (ValueError not assert: survives python -O); silent-drop branch
  replaced by dropped_signals counter + _enforce_dropped_signal_policy
  (MAX_DROPPED_SIGNALS=0, RuntimeError fail-fast). Defence-in-depth:
  gate unreachable while invariant holds.
- Return signature unchanged.

Tests:
- test_main_research_c_v1_1.py: fixtures rebuilt in real-data order
  (entry<=exit); 5 fail-fast negative tests (backdated->ValueError+
  caplog, OPEN-exempt, entry==exit legal, gate direct, threshold==0);
  _reference_dd_and_mult rewritten as genuine O(N2) recomputation.
  FINDING (visible): old test_D was vacuous-green (backdated fixture ->
  all trades silently dropped -> []==[]). 43/43 PASS.
- test_causality_synthetic.py: Test 3/4 expectations updated to
  canonical single-curve semantics (disclosed supersession). 4/4 PASS.

Benchmark re-run (HEAD semantics, full production):
2302T | +2593.26R | MaxDD 5.00R (2.24%) | PF 4.97 | paused=0 |
x1=1994 x0.5=278 x0.25=30 | WR 69.37%. EXACT match to pre-fix HEAD ->
(b)-fix is a semantics disclosure, not a number change; invariant fired
0/2302 on real data (Q5). Pre-run dataset bit-verification: 24/24
SHA256 MATCH. c_v1_1_summary.json regenerated from full run (tracked
copy held dry-run 79T under a canonical label - disclosure-grade
inconsistency corrected).

Ledger: QUARANTINED banner (0899b38 figures preserved verbatim, 12.1);
provenance package benchmark_provenance_c_v1_1_arbitration_b.md;
progress.md echo-artifact restored with process-incident note.

Full suite within tests/: 3F/486P/1S (598.9s) - 2x test_e2e_live_chain
pre-existing disclosed; 1x test_orchestrator_d49 fresh_boot = documented
time-boundary flake REPEAT (D53 open item, activeContext ~1107):
differential 5/5 + 8/8 + 103/103 PASS in isolation, 15m-slot drift only
under full-suite duration; outside this task scope, disclosed not
hidden. Index regenerated intentionally (10.2) from final tree after
pre-commit reformat: 1544 fn, diff limited to this package's files.
Frozen engines + src/ untouched (freeze-rutini clean).
```

**"Single-curve hardening tam-ne-değiştirdi" (kod-düzeyi):** tek operasyon —
`apply_dd_scaling` içindeki **sessiz-drop dalı** (`entry_ts > exit_ts`
trade'leri gözden kayboluyordu) → (1) `ValueError` fail-fast + audit ERROR,
(2) `dropped_signals` sayacı + `_enforce_dropped_signal_policy`
(MAX_DROPPED_SIGNALS=0 → RuntimeError). Return-imzası DEĞİŞMEDİ.
Gerçek-veride invariant 0/2302 ateşlendi → sayılar aynı; değişen
**semantiğin beyanı** (drop-mu-var-hata-mı belirsizliği) kaldırıldı.

### B.1b `benchmark_provenance_c_v1_1_arbitration_b.md` ilk-30 satır

Yol-düzeltmesi: dosya `results/benchmark/` altında DEĞİL —
`memory-bank/benchmark_provenance_c_v1_1_arbitration_b.md` (Hakem'in
varsaydığı yol bir kez daha yanlış; defter bu düzeltmeyi not eder).

```
# BENCHMARK PROVENANCE — C v1.1 ARBITRATION (b) RE-RUN

> Generated: 2026-08-31T17:40:34Z (UTC) by automated verification against
> `memory-bank/dataset_manifest_v1.1.md`. This file is the §8.1
> provenance package for the canonical benchmark re-run recorded in
> `memory-bank/progress.md` (ARBITRATION (b) BENCHMARK RE-RUN).

## 1. Exact code under benchmark

benchmark commit      : <PARENT=34232a1 + UNCOMMITTED WORKING TREE>
                        (run-time truth, kept verbatim; the identical tree
                        was committed afterwards as c66888a — engine bytes
                        at c66888a == engine bytes at run time, modulo
                        line endings, see engine sha256 note)
parent commit         : 34232a16b607e072e90910fb54ad3faa7a7d6131
engine file           : experiment/main_research_c_v1_1.py
engine sha256         : 773e01b1d599b09fee9b1f220bbdfd5d601284917eb91d477a21dc6986372f5d
                        ^ form (i): working-tree bytes (CRLF; Windows
                        checkout, core.autocrlf=true) — the exact bytes the
                        benchmark process imported and executed.
                        form (ii): committed-blob bytes (LF-normalized by
                        git): 78a4bce58c34905000e9a5d0793535c346ca77f22c4e80786b193ae6ca2fa3f5
                        Content-identical modulo line endings (verified:
                        sha256(worktree.replace(CRLF,LF)) == blob sha256).
                        Reproduction: check out c66888a on Windows
                        (autocrlf=true) -> form (i) bytes -> exact-match
                        re-run expected.
frozen engines        : main_research_c_v1_0.py / main_research_d_v1_0.py UNTOUCHED (git diff clean)
python                : 3.12.2 (MSC v.1937 64 bit)
```

### B.2 `experiment/exp_cbdr_time_semantic_alignment.py` — ne yapıyor

Boyut: 315 satır, UNTRACKED. Docstring tam metni (ilk-60'tan):

```
EXP CBDR_TIME_SEMANTIC_ALIGNMENT — 6 major × 2 run = 12 koşum.

Hakem direktifi (2026-09-01):
  RUN A (canonical):  6 feather × raw server-time + mevcut engine
                      → beklenti: toplam fingerprint birebir (2302T +
                      sembol-bazlı dağılım — self-verification)
  RUN B (production): AYNI 6 major, server→UTC dönüştürülmüş dataset
                      + AYNI engine → production-semantics'in
                      6-symbol karşılığı
  RAPOR: sembol-bazlı ZORUNLU — EURUSD alone değil, 6 sembolün hepsi
  Sinyal-zamanlaması (signal-timing): hangi pencerede sinyal geldiği

TEK DEĞİŞKEN disiplini: A/B aynı engine, aynı dataset — yalnızca
zaman-dönüşümü değişir. Bu salt-okunur diagnostik araçtır; freeze-dışı
kod (src/, tests/, index.json) DOKUNULMAZ.

Engine: experiment.main_research_c_v1_0.run_test_a (FROZEN canonical).
```

**Hakem tıntısının hükmü:** EVET — bu dosya tam ③-kış-DST-probe'unun
ta kendisidir; 2026-09-01 direktifli, `server_to_utc_historical`
(production-canonical dönüşüm) ile A/B tek-değişken koşumu tasarlanmış.
Kullanılan import: `src.live.clock.server_to_utc_historical`.
Beklenen çapa: `EXPECTED_FINGERPRINT = {"trades": 2302, "total_pnl": 2875.00, "win_rate": 69.37}`.
Durum: **araç-var, koşum-sonucu defterde yok** (`results/exp_cbdr_time_semantic_alignment.json` untracked mevcut — içeriği bir sonraki kapıda).

---

## C — KAPI-AÇILINCA-İLKE İSTEM — YER İŞARETÇİSİ (içerik OKUNMADI, bilerek)

Gate HOLD'da; Hakem "şimdi-okumak israf" dedi → yalnız envanter:

| Modül | Yol | Satır |
|---|---|---|
| PositionManager | `src/live/position_manager.py` | 266 |
| TradeLifecycle | `src/live/trade_lifecycle.py` | 395 |
| CandleFeed | `src/live/candle_feed.py` | 210 |

İmza-seviyesi ipucu (içerik-değil, yapı-sorusu için — Hakem'in
"tek-slot-mu/paylaşımlı-mı" sorusunun ham cevabı gate-açılışında verilecek):
`position_manager.py` yüzeyinde `get_for_symbol(symbol) -> List[Position]`
ve `restore(positions: List[Position])` var — yani **sembol-başına-LIST**
görünümü; bu, tek-slot-iddiasıyla çelişebilir ya da uyumludur —
hüküm için tam-okuma gerekir, speküle edilmiyor.

---

## D — MASA-HİJYENİ

### D.1 `AGENTS.md` in-flight diff (tamamı — tek ek bloktur)

Komut: `git diff AGENTS.md` → diff BOŞ DEĞİL; `M AGENTS.md` çalışma-ağacında.
İçerik: **yalnız §18'e Aşama-5 eklemesi** (Hakem'in kendi emri — üçlü-kanal
crash-bildirim protokolü). Başka değişiklik YOK.

```
+### Aşama-5: Crash / Fix-Bildirim Üçlü Kanal Zorunluluğu
+
+Her SOAK-CRASH / fatal-error / fix-commit, **ÜÇ kanala AYNI ANDA** bildirilir:
+
+| # | Kanal       | Rol                                          |
+|---|-------------|----------------------------------------------|
+| 1 | Hakem       | Arbitraj — karar yetkisi                      |
+| 2 | Sentezleyici | Luna — sentez-girdisi olarak                |
+| 3 | Owner       | Forexçi — operasyonel-bilgi olarak            |
+
+**Tek-kanal bildirim = eksik-bildirim sayılır.**
+
+> **FORMÜL:** "CRASH BİLDİRİMİ TEK KANALA YÖNLENDİRMEYİN."
+>
+> "Crash yemeyen production sistemi diye bir şey yok;
+> CRASH'İ GİZLİ KIRILAN production sistemi vardır."
```

Usul-notu: defter-hükmü gereği `M AGENTS.md` A6-seti **DIŞINDA**
(paralel-aktör hattı, kendi-commit'inde). Bu dosya da untracked — set-dışı.

### D.2 T0-ham-logları envanteri — ÖNEMLİ DURUM-GERÇEĞİ

Komut: `ls logs/ | grep -iE "t0|soak"`

```
soak_20260901.out
soak_fix_20260901.out
soak_n2_13_20260901.out
soak_n2_13_b1b_20260901.out
soak_n2_15_t0_4_20260902.out   ← T0#4 ÇÖKME-logu (4253 B, 2 Eyl 09:04) — MEVCUT
soak_proof_replay.out
```

**Dürüst-kayıt:** `logs/` altında **T0#5 veya T0#6 ham-çökme-logu YOK.**
Tek ham-crash-logu T0#4'e aittir. Hakem'in D-maddesindeki
"iki-boot-özdeş-imza" ifadesinin repodaki karşılığı: T0#5/T0#6
boot'ları ya hiç-log-dosyası-bırakmadan sonlandı ya log-ları farklı
yolda/adla — defter-arşivi bunu `checkpoint-özeti` seviyesinde taşıyor.
**"Flush-olmadan-kayıp-hipotezi" bu gözlemle GÜÇLENİR** (ham-log-yokluğu
kendisi flush-kayıp iddiasının tutarlı-parçasıdır) ancak T0#7-K5/sync-flush
testi planlı-kapanış olarak duruyor — hüküm-değişikliği yok, etiket-doğrulama:
**DOĞRULANMADI** geçerli.

---

## Dürüstlük-tablosu — GÜNCELLENMİŞ hali (bu paketle)

| Hüküm | Temel | Derece | Değişim |
|---|---|---|---|
| R7-L3 SAME | Cline-grep+dump ↔ bundle-çapaları | çapraz — yüksek | — |
| N2 #19-parite + D62 | Cline-ham-tablolar + bağımsız-aritmetik-kimlik | aritmetik — yüksek | — |
| v1.0/v1.1-mühür | Cline-git-diff-çıktıları | çapraz — yüksek | — |
| ⑧-çelişkisi (D62-çekirdeği) | **artık: ham-JSON-dizimi (A.1) + RAPOR-ham-satırlar (A.2/A.3)** | **birincil — mühürlendi** | **tek-halka → kapandı** |
| c66888a-attribüsyon | commit-msg-tam (B.1a) + provenance-30 (B.1b) | birincil | **B1 kapandı** |
| DST-dosyası kimliği | docstring-ham (B.2) | birincil | **B2 kapandı** |
| T0#5/6-hipotezleri | checkpoint + **yeni-gözlem: ham-log-yokluğu (D.2)** | planlı-kapanış (T0#7) | etiket-doğrulandı |
| pozisyon-sahipliği | yalnız-imza-ipucu (C) | **spekülasyon-YOK — gate-koyulu** | açık-bırakıldı |

**Tek cümle:** A-paketi bundan böyle bu-dosyada birincil; bülten-yayını
için Hakem-tarafı engel kalmadı — kalan tek-yayın-beklemesi Hakem'in
bülten-metninin-kendi-onayıdır.

---

# HAKEM-BİLGİLENDİRME — D137 (2026-09-07): USDCHF/NZDUSD feather-doğrulaması + sonraki-iş

> **Kim:** Copilot (Reis'in ajanı) · **Kime:** Hakem · **Tarih:** 2026-09-07
> **Amaç:** (1) Reis'in USDCHF/NZDUSD operatör-emriyle yürütülen 2-parite
> doğrulama turunun kanıtlı özeti; (2) Reis'in yeni FVG-sorusunun
> yanıtındaki kanıt-zinciri; (3) sonraki-iş önerisi ve Hakem'e kalan
> kontrol noktaları. Tam-rapor: `results/matching_bot_report.md` §10–§10.6.

## B1 — 2-parite testinin detayı (koşu `20260907_150541`)

**Kapsam:** USDCHF + NZDUSD; Reis'in cTrader-export RAW m1'i
(`data/icmarket_raw/`, 977.493 / 977.363 satır, 8/8 RAW aynı aralık
2024-01-01 22:01 → 2026-08-21 20:56) → kanonik mekanizmalarla feather
(`convert_one()` import-only + `resample_15m` verbatim) →
`matching_bot --symbols USDCHF,NZDUSD` (FEED_T0 2026-06-26, SEGMENT
07-10 → 08-21; 37 gün/çift).

**Üretim-zinciri olayı (beyanlı):** İlk 15m denemesi 93 bar üretti —
pandas 3.0.5 `to_datetime` artık `datetime64[us]`; ns-varsayımlı
bucketing 10,4-günlük kovalara savruldu. Düzeltme: converter'da tek
satır ns-pin (`scripts/convert_icmarket_raw_to_feather.py` — araç-
script, üretim kodu değil). Sonrası: USDCHF 65.728 / NZDUSD 65.711
15m bar, validate ALL PASS.

**Sonuç tablosu (tam: rapor §10.3):** MATCH_A gövde **0/0**; sweep
ATTR-modda **0/0** (tol-kaynağına %100 attribüte — A1 donuk-ATR
contract'ı aynen); closed 6/6 (Cmt); B fiyat günleri USDCHF 2 / NZDUSD
3 (gated+Cmt+sınıf-uyumu); C presence 13+13 (segment-sonu kesimi).
**Gövde-paritesi artık 7/7 parite.**

**MATCH_C (NZDUSD 07-14, bl 1.5e-4) — kök-neden §10.6'da mühürlendi:**
21:15 bucket'ında tek 1m bar (21:28, OHLC 0.57437) → kanonik `<3:
continue` bucket'ı düşürür → feather bl=0.57452; cBot platform-resample
drop-etmez → bl=0.57437. **Veri-bozulması değil; iki veri-hattının
resample felsefesi farkı.** cBot ekran-görüntüsü teyidi: width 0.5798%,
swU=True — log-değerleriyle birebir. "Pratikte eşdeğer" hükmü korunur;
kalıcı çözüm (cBot'a ≥3 kuralı) ayrı karar-kalemi, öneri-dışı kayıt.

**Hakem kontrol-1 (band) — §10.5'te kapatıldı:** 65.716–65.741 =
6-parite gerçek min/max (USDCAD/GBPJPY); NZDUSD 65.711 band-altı ama
eksik-bucket sınıf-uyumlu (1.347; sınıf 1.317–1.347 — en-düşük-
likidite major). "ALL PASS" kanonik kurala göre iç-tutarlı
(dropped_buckets NZDUSD=29, USDCHF=15).

## B2 — Reis'in FVG-sorusu: kanıt-zinciri (rapora yeni bölüm değil; kanıt burada)

**Soru:** "Her parite için FVG-size istatistiği? Bir swing bacağında 4
FVG görüyoruz — hangisini hedef almalı? Bizde zaten post-sweep + bakire
koşulu var."

**Kanıt-1 (slot-seçimi kodda — birincil):**
`experiment/gemini_benchmark.py:301-422` — `detect_fvgs(lookback=100)`
tüm adayları toplar; filtre zinciri sırayla: sweep-sonrası (real_index >
sweep) → yön-uyumu → invalidated-değil → `_is_fresh_fvg` (bakire) →
EQ-bütün-bölge → touch → min-risk; **ilk-uygun-FVG'de `break`**.
Yani "4 FVG'den hangisi" sorusunun cevabı zaten üretim-semantiğinde:
**sweep'e en yakın (#1) slot.** Aynı sabitler production'da:
`FVG_MIN_SIZE_ATR_MULT=0.06` (`experiment/config.py:21`;
`strategy_runtime.py:631` aynı sabiti kullanır).

**Kanıt-2 (EXP5B, hafızadan):** #1 slot N=434 WR 58.3% +335.9R vs #2
slot N=33 WR 51.5% +2.3R — #1 lehine.

**Kanıt-3 (trade-dump'tan parite-bazlı FVG-size, n=102):**
`results/benchmark/abfix_eq_trades.json` repo-reset'te silinmişti
(71df268); git'ten kurtarıldı (746504b commit'inde
`archive/results_20260904/benchmark/repro_icmarket/` yolu), analiz
edildi, geçici-dosya silindi (report-only, iz-yok). ATR-normalize
`zone_size_atr` medyanları: USDJPY 0.414 > GBPUSD 0.245 ≈ USDCAD
0.250 ≈ EURUSD 0.256 ≈ AUDUSD 0.231 > GBPJPY 0.202. **min 0.06×ATR
zaten ATR-normalize → parite-farkı büyük ölçüde elenmiş**; USDJPY bile
yalnız ~1.6×. Parite-özel eşik İÇİN n=102 (grup-başına 8–9 trade)
istatistiksel-dayanaksız → **parite-bazlı eşik önerisi YOK.**

**Kanıt-4 (size vs outcome, tanımlayıcı — filtre-önerisi DEĞİL):**
global tercile (0.210/0.381): küçük WR 70.6% +216.7R; orta 58.8%
+35.9R; büyük 50.0% +18.5R — monoton azalma. Ama parite-içi
medyan-split karışık (AUDUSD büyük-iyi, GBPUSD küçük-çok-iyi) →
"boyut-küçükse-iyi" evrensel kural çıkmaz.

**Dikkat-notu (dürüstlük):** trade-dump'ta sweep→FVG **bar-mesafesi**
slot1 (≤2 bar) WR 38.5% vs slot3+ (6+ bar) WR 64.4% görünüyor — bu
**slot-sırası değil, mesafe-metriği**; EXP5B'nin #1-slot bulgusuyla
çelişmez (farklı değişken). Karıştırılmamalı.

## B3 — Sonraki-iş önerisi (Reis kararı + Hakem kontrol-üçgeni)

1. **Config-yazımı (7 parite):** Hakem yeşil-ışık verdi (§10.4);
   sıra **Reis'in yazılı onayında**. Değerler rev.4 §2/§5 aynen:
   USDCHF 0.152/0.254/0.374; NZDUSD 0.182/0.365/0.520. Bu bot yazım
   yapmaz — Reis onayı olmadan config'e hiçbir şey yazılmaz.
2. **Trade-gating DEBT:** Adım-C ön-koşulu aynen yürürlükte (§9.2);
   minimum-kapsam: discard'ta audit-satırı
   (`CYCLE_DISCARDED` / `STATE(moment=cycle_discarded)` — N2#23-b
   deseni). Soak-açılışından önce planlanmalı.
3. **FVG-size çalışması (opsiyonel, öncelik-düşük):** Mevcut kanıt
   tanımlayıcı-yeterli; periyodik yeniden-ölçüm (trade-arttıkça)
   önerilir, eşik-tuningleme değil. `build_fvg_size_dataset_v2.py`
   mevcut, girdi-dataset'i reset'te gitmiş — gerekirse yeniden
   üretilir.
4. **Hakem'e kalan kontrol-noktaları:** (a) §10.5/§10.6 kök-neden
   hükümlerinin ratifiye-edilmesi (band-sınıfı + drop-kuralı); (b) B2
   kanıt-zincirinin slot-seçimi okumasına itiraz var mı (break-on-first
   = #1-slot lehine); (c) C-presence 13+13 segment-sonu kesiminin
   "veri-kalitesi-değil" etiketine onay.

## Kanıt-konumları (Hakem doğrulaması için)
- Rapor: `results/matching_bot_report.md` §10–§10.6
- Artefakt: `results/matching_bot/20260907_150541_matching.json` (2-parite)
- Taban: `results/matching_bot/20260907_134038_matching.json` (5-parite)
- abfix-eq trade-dump: git `746504b:archive/results_20260904/benchmark/repro_icmarket/abfix_eq_trades.json`
- Kod: `experiment/gemini_benchmark.py:301-422`; `experiment/config.py:21`; `src/live/strategy_runtime.py:631`
- Hafıza: `/memories/repo/sniper_forex_fvg_size_evidence.md` (yeni); `sniper_forex_d135_matching_bot.md` D136-DATA md.6-7

## D139 — Config-yazımı İCRASI (2026-09-08, Cline)

**Durum:** Reis'in yazılı onayı geldi (D138 §3 beklentisi karşılandı).
Config-yazımı icra edildi — **data-only**, tüketici-kod YOK.

**Yazılan dosya:** `src/config/cbdr_band_config.py` (YENİ, untracked)
- `CBDR_BAND_PERCENTILES`: 7 parite p25/p75/p90 + median + verified-bayrağı
- Değerler rev.4 §2 tablosundan birebir (3 anlamlı basamak, §123 kuralı):
  USDCAD 0.0841/0.143/0.210 | EURUSD 0.0866/0.166/0.267 |
  GBPUSD 0.0996/0.184/0.281 | USDJPY 0.106/0.223/0.361 |
  AUDUSD 0.145/0.290/0.466 | USDCHF 0.152/0.254/0.374 (verified=False) |
  NZDUSD 0.182/0.365/0.520 (verified=False)
- `classify_width_regime()`: sıkışma/tipik/yükselmiş/makro (rev.4 §117-120)
- Cmt-kovası YOK (matching_bot_report.md §6.2: üretimde uygulanamaz,
  SessionManager discard eder) — bilinçli çıkarma, kayıt burada.
- USDCHF/NZDUSD: feather yok → eşleşme-doğrulaması yapılamadı →
  **etiketli-giriş (Seçenek B)**, verified=False; eşleşme-koşusu
  yapılana kadar bayrak False kalır.

**Mekanizma-bulgusu (önemli):** Percentile'ları tüketen production-kod
repo'da YOK (src/ + experiment/ taraması boş; cBot scaffold config.json
6-param, percentile-alanı yok). rev.4 §7.2 "uygulama ayrı iş" hükmü
doğrulandı. Bu dosya hiçbir modül tarafından import edilmiyor —
wiring ayrı, Reis-onaylı görevdir (A1-RED + AGENTS §2.2 uyumlu).

**Doğrulama:**
- ruff check + format: temiz
- import + değer-echo: 7 parite birebir rev.4 §2 ile eşleşti
- Test: kapsam-beyanlı koşu — 9 collect-hatalı dosya (archive-taşınması
  5223d59 kaynaklı ModuleNotFoundError, pre-existing) + parity-dosyaları
  (aynı kök) hariç: **539 passed, 2 skipped**. Tam-sayımda 14 failed
  (hepsi `experiment.main_research_c_v1_0` silinmesi kaynaklı,
  pre-existing; config-dosyası hiçbir test tarafından import edilmiyor).
- Diferansiyel: parity-testleri config'siz de düşüyor (stash-deneği
  gereksizdi — dosya untracked, test-import zincirinde yok).

**Commit/push:** YAPILMADI (AGENTS §9.2 — yazılı onay ayrı istenir).
Çalışma-ağacında bekliyor; commit-scope önerisi: yalnız
`src/config/cbdr_band_config.py` + bu D139 bloğu.

## D140 — Adım A.3 env-kurulumu (2026-09-07, Cline)

**Reis iletimi:** Access token (§5 kalem-3, A.3 başlatma). Reis'in
§5 güncellemesi: token-uyarısı GERİ ALINDI (aşırı-tepki); KYC TAMAM.

**Yapılan:**
1. `.env` → `CTRADER_ACCESS_TOKEN` eklendi (dosya gitignore'da;
   token repoya ASLA girmiyor — hibrit-disiplin korunuyor).
2. `src/config/ctrader_config.py` → `get_ctrader_config()` artık
   `access_token` alanını okuyor (optional; mevcut-mekanizma §2.2).
   ruff temiz; env→config zinciri doğrulandı (len=43 echo'da değer yok).
3. `token_cache.json` hâlâ boş (access_token=None) — connection.py
   `_send_account_auth()` cache'ten okur; Reis'in token'ı cache'e
   yazılmadı (A.3 koşusu Reis-onaylı ayrı adım).

**A.3 BAŞLATILAMAZ — engel beyanı (fail-loud):**
- `CTRADER_CLIENT_ID`: BOŞ
- `CTRADER_CLIENT_SECRET`: BOŞ
ProtoOAApplicationAuthReq bu ikisi olmadan gönderilemez (connection.py
:126-127). Reis'in §5 tablosunda "KYC TAMAMLANDI" var → Playground >
Credentials'tan client_id/secret bekleniyor. Access token tek başına
yeterli değil: app-auth ÖNCE, account-auth (token) SONRA.

**Not:** Access-token'ın cTrader'daki geçerliliği app-auth sonrası
hesap-auth ile sınanacak; başarısızsa refresh-token akışı (§2) devreye
girer.

**Commit/push:** YAPILMADI. Çalışma-ağacı: .env (untracked, ignore),
ctrader_config.py (modified, +2 satır), cbdr_band_config.py (D139),
memory-bank blokları.

## D141 — A.3 ilk-canlı-koşu: kısmi-başarı + hesap-uyumsuzluğu (2026-09-07)

**Reis client_id + client_secret'i .env'ye doldurdu → koşu yapıldı.**

**CANITILAN (gerçek production-path, src/ctrader/connection.py):**
1. TCP bağlantı (demo.ctraderapi.com) → CONNECTED ✓
2. ProtoOAApplicationAuthReq (client_id+secret) → APP_AUTH_RES ✓
   **Credentials GEÇERLİ — KYC/onay zinciri çalışıyor.**
3. Account-auth (access token) → ACCOUNT_AUTH_RES ✓ (token kabul)
4. ProtoOASymbolsListReq → **REDDİ: ProtoOAErrorRes
   `INVALID_REQUEST: Trading account is not authorized`**

**KÖK-NEDEN:** `CH_CTID_TRADER_ACCOUNT_NOT_FOUND — "Unable to find
cTID trader account with id=10103194"`. `.env'deki CTRADER_ACCOUNT_ID
(10103194) access-token'ın ait olduğu cTID'ye bağlı hesap DEĞİL.
Değer D126 unit-test'lerinden (tests/test_ctrader_connection_unit.py:34)
env'ye kopyalanmış placeholder — gerçek hesap değil.

**Ek bulgu (kod-düzeltmesi gerektiği kanıtlandı):** Symbols-isteği
APP_AUTH_RES anında gönderilirse 'not authorized' reddi gelir — doğru
sıra app-auth → account-auth SONRASI symbols-istek (connection.py'de
request_symbols_list'in çağrı-sırası wiring-taskında dikkate alınmalı;
A.1-RED kapsamında kod-değişikliği yapılmadı, teşhis-koşularında
sıra düzeltildi).

**A.3'ün kalan engeli: Reis'ten istenen.** İki seçenek:
- (a) openapi.ctrader.com → Applications → uygulama-sayfasındaki
  hesap-listesinden demo hesabın ctidTraderAccountId'si;
- (b) .env'deki CTRADER_ACCOUNT_ID'yi o değerle güncelle.
Gelince: symbols-list → BTCUSD symbolId + MT5 session karşılaştırması (G2).

**Env-güvenlik:** değerler repoya girmiyor (.env gitignore'da);
test-dosyalarına token/secret yazılmadı; teşhis-koşuları /tmp altında.

**Commit/push:** YAPILMADI. Çalışma-ağacı: .env (Reis doldurdu,
untracked), src/config/ctrader_config.py (+access_token, D140),
cbdr_band_config.py (D139), memory-bank blokları.

## D142 — A.3 BAŞARILI: traderLogin ≠ ctidTraderAccountId çözümü (2026-09-07)

**Çözüm-yolu:** ProtoOAGetAccountListByAccessTokenRes (demo-host, hata-sonrası
break yok) → token'ın cTID'sine bağlı tek hesap göründü:

    ctidTraderAccountId=48407657 | isLive=False (DEMO) | traderLogin=10103194

**Kök-neden kesinleşti:** 10103194 = **traderLogin** (kullanıcı-görür hesap-no);
API'nin istediği ctidTraderAccountId = **48407657**. İki numara farklı.
.env düzeltildi: CTRADER_ACCOUNT_ID=48407657.

**Yeniden-koşu (a3_final_test, doğru-id ile) — A3_RESULT: PASS:**
1. CONNECTED ✓  2. APP_AUTH_RES ✓  3. ACCOUNT_AUTH_RES ✓ (errors=0)
4. Symbols-list (account-auth SONRASI) ✓ → **BTCUSD symbolId=10026**

**Kabul-kriterleri (docs/ctrader_integration_plan_v2.md §4):**
- Auth → token_cache.json access_token dolu ✓
- BTCUSD symbolId bulundu ✓ (10026)
- Kalan: G2 (cTrader vs MT5 session karşılaştırması), rate-limit 429 gözlemi, Test 6/6

**Ders (wiring-notu):** account-auth başarısızken bile APP_AUTH +
ACCOUNT_AUTH-res-sinyalleri görünebilir — ProtoOAErrorRes içerik-kontrolü
zorunlu (D141 dersinin teyidi). Hesap-listesi isteği hata-sonrası da
yanıtlanıyor; break-erken-exit kanıtı kaybına yol açar.

**Env-güvenlik:** .env gitignore'da; token/secret repoya girmedi.
**Commit/push:** YAPILMADI (§9.2).

### D142-EK — test-real-env tutarlılığı (2026-09-07)

`.env` account-id değişimi (10103194→48407657) unit-test'i kırdı:
`test_config_loads_from_real_env_without_hanging_on_empty_fields`
gerçek-env değerine DONUK assert ediyordu (tests/...unit.py:53).

**Düzeltme (test-convenience DEĞİL — assertion-scope düzeltmesi):**
- Gerçek-env testi: `account_id.isdigit()` (wiring + yapısal-şekil);
  deployment-değeri .env'ye ait, test'e donuk sabit DEĞİL.
- DEMO_CONFIG placeholder'ına D142 notu eklendi (10103194 = traderLogin,
  ctidTraderAccountId DEĞİL).

**Sonuç:** 12 passed (önce 1 failed, 11 passed). Suite yeşil; kırmızı-gizleme yok.

## D143 — G2 TAMAMLANDI: cTrader BTCUSD schedule canlı-çekim (2026-09-07)

**Yol:** ProtoOASymbolByIdReq (symbolId=10026) → ProtoOASymbolByIdRes.
SymbolsListRes light-symbol döndürüyor (schedule taşımıyor) — detay
SymbolById'da. Alan-adları DESCRIPTOR'dan kesinleştirildi:
ProtoOASymbol.schedule = repeated ProtoOAInterval {startSecond, endSecond},
+ scheduleTimeZone, tradingMode. (D141-dersi teyit: getattr/DESCRIPTOR,
SDK-adı varsayma.)

**cTrader BTCUSD (IC Markets demo, scheduleTimeZone=America/New_York,
tradingMode=0/ACTIVE), 8 interval (hafta-offset saniye, 0=Paz 00:00):**

| Interval | startSecond | endSecond |
|---|---|---|
| Paz 00:00→16:59 | 0 | 61140 |
| Paz 17:05→Pzt 16:59 | 61500 | 147540 |
| Pzt 17:05→Sal 16:59 | 147900 | 233940 |
| Sal 17:05→Car 16:59 | 234300 | 320340 |
| Car 17:05→Per 16:59 | 320700 | 406740 |
| Per 17:05→Cum 16:55 | 407100 | 492900 |
| Cum 17:45→Cmt 16:59 | 495900 | 579540 |
| Cmt 17:05→Cmt 23:59 | 579900 | 604799 |

**MT5-tablosuyla uyum (G2 kabul):** 7 gün/hafta işlem ✓; günlük 17:00-17:05
NY kapanış/açılış molası ✓ (D126 cTrader-tablosundaki 00:05 server-time
= 17:05 NY önceki gün — zaman-dilimi-normalizasyonu netleşti); Cum 16:55
kapanış ✓; Cmt 17:45 geç-açılış (MT5 Cmt ~00:00-00:57 mola karşılığı) ✓.

**Zaman-dilimi-notu (§6.3):** cTrader schedule America/New_York; MT5 tick
UTC-epoch. Kalıcı-normalizasyon wiring-taskında (A1-RED kapsamı dışı).

**Kabul-tablosu (plan §4) güncel:** Auth ✓ | Heartbeat (log-kanıtı var,
10s) ✓ | BTCUSD symbolId=10026 ✓ | **G2 ✓ (bu blok)** | Rate-limit 429
mock-testi ☐ | Test 6/6 → unit 12/12 ✓.

**Commit/push:** YAPILMADI (§9.2). Teşhis-scriptleri /tmp'de.

## D144 — A.3 tam-suite durumu + kapsam-beyanı (2026-09-07)

**Suite:** 558 toplandı; 10 modül toplanamıyor (9 collect-error + 1
test_main_research_c_v1_1) — TÜMÜNÜN ortak kökü:
`ModuleNotFoundError: No module named 'experiment.main_research_c_v1_0'`
(experiment-modülü çalışma-ağacında yok — archive/soak-frozen dönemi
kalıntısı; bu görevle İLGİSİZ, önceden-mevcut).

**Koşulan kapsam (10 modül hariç):** 14 failed, 543 passed, 2 skipped.
14 FAILED'in 8'i aynı kök (test_parity_6majors — main_research_c_v1_0
importu). Kalan 6 (live_parity_gate 1, live_strategy_runtime 2,
m1_ingestion_parity 4'ün 4'ü) — parity-ailesi; muhtemel aynı bağımlılık
zinciri, ayrı-diferansiyel gerekli.

**FAILURE beyanı (§4.4):**
Scope: dış (parity/experiment-modül-eksikleri — A.3/ctrader-kapsamı dışı)
Status: önceden-mevcut (bu fazda src/experiment değişikliği YOK; git
status: live/* + test_orchestrator_startup değişiklikleri ayrı iş-ağacı)
Evidence: pytest-çıktısı + git-status; parity-FAILED kökü
ModuleNotFoundError experiment.main_research_c_v1_0
Action: ayrı-görev (experiment-modülü restore/promote kararı — Reis)

**Ctrader-kapsamı net-durum:** tests/test_ctrader_connection_unit.py
**12/12 PASS** (suite-yeniden-koşuda teyitli).

## D145 — Rate-limit 429 gözlem-testi (A.3 son-kriter) (2026-09-07)

**Yöntem:** Canlı salt-okur istek-fırtınası (ProtoOASymbolsListReq — emir
YOK). 3 koşu: 60 / 300 / 300 istek (SDK numberOfMessagesToSendPerSecond=5
istemci-clamp'i aktif → 300 istek ≈ 60sn'de akar).

**Sonuçlar:**
- v1: 60 gönderildi → 60 ProtoOASymbolsListRes, 0 hata.
- v2: 300 gönderildi → 150 RES, 0 ProtoOAErrorRes, 150 yanıt-yok.
- v3 (errback-sayacıyla): 300 gönderildi → 150 RES + **150 timeout-errback**
  (responseTimeoutInSeconds=30'da yanıt-gelmeyen istekler), 0 429.

**YORUM (kanıta-dayalı):**
1. Demo-host'ta symbols-istek-fırtınasıyla TRADING_BAD_RATE_LIMIT
   TETİKLENEMEDİ — sunucu limiti bu yoğunlukta görünmedi (demo-host
   limiti daha yüksek veya symbols-istekleri limit-dışı).
2. Kayıp-yanıtlar sessiz-düşürme değil, deferred-timeout (errback-kanıtı:
   150/150). Yani istemci, yanıt-gelmeyen istekleri timeout'la temizliyor —
   askıda-kalma YOK (fail-safe davranış ✓).
3. ProtoOAErrorRes alanları DESCRIPTOR'dan teyitli: errorCode/description/
   maintenanceEndTimestamp — 429 gelirse bu üçlüyle taşınacak. SDK
   ProtoErrorCode enum'unda TRADING_BAD_RATE_LIMIT YOK (enum 10 değerli;
   errorCode serbest-string olarak gelir — içerik-kontrolü string-bazlı
   yapılmalı, enum-eşleşmesi BEKLENMEZ).

**Kabul-kararı:** Plan §4'teki "Rate limit | 429 handling test edildi |
Mock 429 → retryAfter" kriteri — canlı-tetikleme YAPILAMADI (sunucu
limiti görünmedi); mock-yolu ayrı-unit-görevi olarak kalmalı (connection.py
hata-yolunda 429-özel-dal YOK — mevcut davranış: ProtoOAErrorRes MESSAGE
olarak event_queue'ya düşer, tüketici görür). Bu, §2.2'ye uygun: yeni
paralel-429-mekanizması İCRA EDİLMEDİ; gözlem-kanıtı + mevcut-yol-beyanı
yeterli. Kalan mock-unit-testi ayrı-görev (Reis-kararı).

**A.3 KABUL-TABLOSU (plan §4) — FINAL:**
| Kriter | Durum |
|---|---|
| Auth → token_cache dolu | ✓ |
| Heartbeat 10s | ✓ |
| BTCUSD symbolId | ✓ 10026 |
| G2 session karşılaştırma | ✓ D143 |
| Rate-limit 429 | ◐ canlı-tetiklenemedi; timeout-fail-safe ✓; mock-unit ayrı-görev |
| Test 6/6 | ✓ ctrader-unit 12/12 |

**Commit/push:** YAPILMADI (§9.2). Teşhis-scriptleri /tmp'de.

---

# HAKEM RAPORU — A.3 cTrader Open API Entegrasyon-Testi (2026-09-07)

**Kapsam:** D139–D145 (config-yazımı → env-kurulumu → canlı-bağlantı →
G2 → 429-gözlem). Reis-direktifi: "iş bitiminde Hakem'e raporlarsın."

## 1. TAMAMLANANLAR (kanıtlı)

| # | İş | Kanıt |
|---|---|---|
| D139 | cbdr_band_config.py (7-parite percentile) | ruff temiz; 539/2 kapsam-beyanlı test |
| D140 | .env 6-anahtar + access_token config-okuma | validate_ctrader_config PASS |
| D141 | İlk-canlı-koşu: app-auth ✓, account-id uyumsuz | CH_CTID_TRADER_ACCOUNT_NOT_FOUND |
| D142 | traderLogin≠ctidTraderAccountId çözümü; .env=48407657; A3_RESULT: PASS; BTCUSD symbolId=10026; unit 12/12 | acctlist-res + a3_final_test |
| D143 | G2: cTrader BTCUSD schedule (8 interval, America/New_York) ↔ MT5 tablo-uyumu | ProtoOASymbolByIdReq canlı |
| D145 | 429-gözlem: 300 istek-fırtınası → 0×429; 150 timeout-errback (fail-safe ✓) | rl429 v1-v3 |

## 2. KABUL-TABLOSU (docs/ctrader_integration_plan_v2.md §4)

Auth ✓ | Heartbeat ✓ | BTCUSD symbolId=10026 ✓ | G2 ✓ | Rate-limit ◐
(canlı-tetiklenemedi; mock-unit ayrı-görev) | Test: ctrader-unit 12/12 ✓.

## 3. AÇIK KALEMLER (Reis-kararı)

1. **Mock-429 unit-testi** — canlı-tetikleme yetersiz; connection.py'de
   429-özel-dal yok (ProtoOAErrorRes MESSAGE olarak kuyruğa düşer —
   tüketici-görür). Yeni paralel-mekanizma icra EDİLMEDİ (§2.2).
2. **experiment.main_research_c_v1_0 eksik** — 10 test-modülü toplanamıyor,
   14 parity-FAILED aynı kök. Kapsam-dışı/önceden-mevcut (D144 kanıtlı).
   Restore/promote kararı Reis'in.
3. **Wiring-taskı** — request_symbols_list çağrı-sırası (account-auth
   SONRASI) + scheduleTimeZone (America/New_York) normalizasyonu.
4. **Token-rotate** — access token sohbette açık-metin iletildi (D140);
   iş tamamlandığından rotate ÖNERİLİR.

## 4. GÜVENLİK / PROVENANCE

- .env + token_cache.json gitignore'da; token/secret repoya girmedi.
- Tüm teşhis-scriptleri /tmp'de (repo-dosyası DEĞİL).
- Commit/push YAPILMADI (§9.2 — yazılı-onay bekler).
- Çalışma-ağacı: src/config/ctrader_config.py (D140), tests/
  test_ctrader_connection_unit.py (D142-EK), memory-bank blokları,
  src/config/cbdr_band_config.py (D139, untracked).

## D146 — İş-7 T0#4 karar-materyali (öncelik-onaylı; KOD-DEĞİŞİKLİĞİ YOK) (2026-09-07)

**Onaylanan sıra:** 7 (T0#4 boot-kararı) → 3 (band-wiring) → 4 (cTrader
wiring). Bu blok İş-7'nin karar-materyali — §7 (heartbeat/safety
invariant) Hakem-ratifiyesiz değişmez.

**Production-path kanıtı (codebase-memory + satır-okuma):**

1. **Retry-bütçesi ZATEN YÜKSELTİLMİŞ (N2 #15-b):** ~0.35s → 8 deneme /
   ~6.4s worst-case (0.05·2^0..2^6=6.35s) — `src/live/atomic_write.py:44-45`
   (_TMP_WRITE_RETRIES=8, _TMP_RETRY_BASE_SLEEP=0.05). LOCK_STALE_SEC=900'ün
   %0.7'si — §7.4 invariant KORUNUYOR.
2. **Heartbeat-write ZATEN NON-FATAL (N2 #17 Katman-4):**
   `orchestrator.py` Lock.heartbeat() — OSError → _write_degraded latch +
   LOUD (crash-log + WRITE_BLOCK audit) + sonraki-tick retry. Boot-takeover
   write-failure FATL (fail-fast) — ayrı-yol, dokunulmadı.
3. **Mekanizma tmp+rename'den IN-PLACE'e çevrilmiş (N2 #17):** rename-
   overwrite hot-path'ten tamamen çıkarıldı; T0#6 aynı-WinError
   Defender-exclusion'la da tekrarlamıştı. K1 ladder + K3 on_block
   in-place yolda da korunuyor.
4. **Katman-1 probe ZATEN VAR (N2 #17):** ilk başarısız denemede
   Restart-Manager handle-holder listesi WRITE_BLOCK'a gömülüyor.

**T0#4 çökmesinin İKİ kökü (D136-dönemi analizi):** (a) tmp-çakışması —
PID-unique tmp ile ÇÖZÜLDÜ; (b) dış-handle (AV) target-kilidi — 0.35s
bütçesi yetmedi. (b)'nin KOD-YANITI Artık HAZIR (madde-1: 6.4s bütçe +
madde-2: non-fatal heartbeat + madde-3: rename kaldırıldı).

**HAKEM KARAR-SORUSU (activeContext'teki açık-soru):** "retry-bütçesi
artırılmalı mı (0.35s→5s) YOKSA heartbeat non-fatal'e mi?" →
**KOD-KANITI: İKİSİ DE YAPILMIŞ** (6.4s bütçe + non-fatal + in-place).
Açık-soru ZAMAN-AŞIMINA (stale) uğramış görünüyor — N2 #17 fix-seti
iki yaniti de içeriyor. HAKEM'DEN İSTENEN: (i) bu kanıtı ratifiye et;
(ii) T0#4-sonrası ilk boot'u ONAYLA (yeni-soak ön-şartı).

**Sonraki-adım (ratifiye sonrası):** İş-3 (band-config tüketici-wiring).

## D147 — HAKEM HÜKMÜ RATİFİYE (2026-09-07)

- §1 D146 kanıtı RATİFİYE (stale açık-soru kapandı).
- §2 T0#4-sonrası ilk boot: KOŞULLU ONAY — §7.4 invariant + boot-takeover
  FATL + anomali-izleme + audit kanıtı (STARTUP/MT5_CONNECT/STATE).
- §3 İş-3 başlatma ONAYLANDI (§2.2 uyumlu).
- Reis ek-hüküm: "config daha yazmadık mı? onay tabi ama push yapma,
  commit le sadece" → push YOK, commit-only.

### Reis'in "config daha yazmadık mı?" CEVABI (D146-D147 arası durum)

- **cbdr_band_config.py VAR** (D139, untracked): 7-parite percentile
  tablosu — USDCHF/NZDUSD verified=False; Cmt kovası bilinçli-yok.
- **"Yazılmayan" ne:** TÜKETİCİ-WIRING (İş-3). Config-dosyası veri;
  onu okuyup production'a bağlayan kod YOK. Ayrıca: USDCHF/NZDUSD
  doğrulama (Reis-veri bekliyor) + Reis'in kalite-onayı (açık-kalem-4).
- Dolayısıyla: "config yazıldı ama canlıya bağlı değil" — İş-3 bu
  bağlantıyı kuracak; tüketici olmadan config ölü-veri.

### D148 — İŞ-3 TARAMA-SONUCU (wiring-design öncesi §2.2 haritası; KOD-DEĞİŞİKLİĞİ YOK) (2026-09-07)

**Mevcut-mekanizma haritası (codebase-memory + satır-okuma):**

1. **`get_cbdr_multiplier(symbol, cbdr_width_pct)` (D129 §3,
   `src/live/risk.py:200`):** placeholder — her zaman 1.0; docstring
   kalibrasyon geldiğinde bucket-scan şemasını tarif ediyor.
   **Production-çağrı-yeri YOK** (yalnız cbot core doc-referansı +
   `tests/test_cbdr_placeholder.py`).
2. **`cbdr_band_config.py` (D139):** percentile-tablosu +
   `classify_width_regime()` + `get_band_percentiles()` — tüketicisiz
   (data-only, beklendiği gibi).
3. **Signal (strategy_runtime.py:70):** cbdr-width taşımıyor.
   `SessionManager.cbdr.body_high/body_low` → width = (BH-BL)/BL*100
   kaynak mevcut (cbot core'daki formülle aynı).
4. **RiskManager.evaluate çağrı-yerleri:** live_runner.py:445,
   signal_runner.py:140, paper.py:494 — üçü de cbdr_width geçmiyor.
5. **`src/backtest/matching_bot.py`:** calibration-verify aracı —
   percentile'ları birebir aynı kaynak-tablodan üretir (§2.2 uyumlu,
   paralel-mekanizma DEĞİL).

**İş-3 tasarım-bulgusu:** D129'da bilinçli-ertelenen `get_cbdr_multiplier`
kalibrasyonu ARTIK MÜMKÜN (D139 tablosu mevcut) — natural-wiring noktası
bu mevcut placeholder'ın gerçek-implementasyonudur (paralel-mekanizma
İCRA ETMEDEN). Çağrı-yeri eklenmesi (live_runner/paper/signal_runner →
width hesaplayıp geçirme) ayrı-adım; Reis-onayına sunulacak tasarım:
(a) multiplier bucket-şeması mı, (b) regime-classification (4-band)
mi, (c) ikisi ayrı-yetkiler mi — rev.4 §117-123 ile eşleşme Hakem'e
soru.

### D149 — COMMIT KAYDI (commit-only; PUSH YOK) (2026-09-07)

**Commit:** `108f16c` — "N2/P1 hardening + D139 band-config + D140-D145
cTrader module (D147 ratified)". Reis-hükmü: commit-LE, push-YAPMA.

**Commit-set:** tek-commit `108f16c` (47 dosya, +6417/−44). Parent:
`521c7f0` (= origin/main; push'ta 1-commit ilerleme olur — onay-isteği
Reis'te).

**Kapsam-beyan:** N2 #15-b/#17/#21/#23/#24 hardening + P1 grubu +
D102-BTI presets + D139 band-config + D140-D145 cTrader modülü +
memory-bank D139-D149 + ruff.toml scaffold-ignore + connection.py
dead-param fix. `.env`/`token_cache.json` DAHİL DEĞİL (gitignore).
`__pycache__` hariç tutuldu.

**Hook-seremonisi (5 deneme — her blok çözüldü, sessiz-geçiş yok):**
1) ruff import-sort (sizing.py fix) + ruff-format 5-dosya → re-stage +
   re-test (35/35). 2) vulture: connection.py:216 `timeout` ölü-param →
   repo-taraması tek-referans; param SİLİNDİ (davranış-değişmedi,
   get_nowait non-blocking; §4.4 görünür-fix) + 12/12 re-test.
3) ruff-format robot_wrapper.py (verbatim-shim) → re-stage.
4) ruff F403/F405 scaffold-shim → ruff.toml per-file-ignore
   (upstream-faithful gerekçeli). 5) end-of-file 3-dosya fix → re-stage.
Final: ruff+ruff-format+vulture+mypy TAMAMI PASS → commit alındı.

**Test-toplamı (targeted):** 26 + 32(+1 skip) + 22 + 28 + 19 + 35
(post-hook) + 12 = PASS. Pre-existing 14-parity-FAILED kapsam-dışı
(D144 beyanı geçerli — experiment-archive kökü).

**PUSH DURUMU:** YAPILMADI — §9.2 yazılı-onay bekliyor. Öneri-commit-set:
`108f16c` (tek). Reis-onayı gelirse push + ls-remote + origin/main..HEAD
boşluk-dogrulama + ledger-kaydı yapılacak.

## D151 — PUSH KAYDI (Reis yazılı-onayı: "1.-2.-3 OK") (2026-09-07)

**Onay:** Reis D150-hükmüne "3 de uygun 1.-2.-3 OK" — (1) push-onayı,
(2) multiplier 0.0/1.0/1.2/1.5, (3) ilk-boot emri. Üçü de yazılı.

**Push-provenance (§9.3):**
- who: Cline (icra-ajanı), Reis-yazılı-onayıyla
- what: N2/P1 hardening + D139 band-config + D140-D145 cTrader + ledger
- when: 2026-09-07
- commits: `108f16c` + `bf77511` (onaylı-set = push edilen-set, birebir)
- remote: origin/main (github.com/ahmetonurof-lab/sniper_forex)
- verification: `521c7f0..bf77511 main -> main`, exit 0;
  origin/main..HEAD = 0 (gap-boş); ls-remote = bf77511 = local HEAD;
  tracked-diff = 0 (temiz)

**Sonraki-adım (aynı-onay):** (2) get_cbdr_multiplier gerçek-implementasyon
(rejim→multiplier zinciri; Hakem D150 §2 tasarımı) → commit-only (yeni-set
için push-onayı YENİDEN istenir — §9.5 set-değişimi=kuralları) → (3) ilk-boot.

## D152 — İŞ-3 KOD: get_cbdr_multiplier GERÇEK-IMPLEMENTASYON (commit-only) (2026-09-07)

**Reis-onayı (D150: "3 de uygun 1.-2.-3 OK"):** push + multiplier(0.0/1.0/1.2/1.5)
+ ilk-boot — üç-karar yazılı alındı.

**Push önce tamamlandı (D151):** `108f16c`+`bf77511` → origin/main,
gap-0, ls-remote=HEAD doğrulandı.

**Kod-değişikliği (İş-3 çekirdeği):**
- `src/live/risk.py`: `get_cbdr_multiplier` placeholder→gerçek. Zincir
  (Hakem D150 §2): `classify_width_regime(symbol, w)` → `CBDR_REGIME_MULTIPLIER[regime]`.
  Tablo: sikisma=0.0 (fail-closed), tipik=1.0, yukselmis=1.2, makro=1.5.
  Import: `src.config.cbdr_band_config` (tek-kaynak, §2.2 — paralel-mekanizma yok).
- `tests/test_cbdr_multiplier.py` (YENİ, 11-test): Reis-tablosu birebir-beyanı +
  7-çift × 6-bant-sınırı parametrize (sınırlar percentile'dan TÜRETİLİR — donuk-değer
  değil) + verified-orta-nokta + bilinmeyen-sembol fail-loud + uç-değerler.
- `tests/test_cbdr_placeholder.py` SİLİNDİ — placeholder-iddiası artık YANLIŞ;
  test-ihtilafı bırakılmadı (görünür-değişim, sessiz-değil).

**Kapsam-notu:** evaluate()-yolunda multiplier-uygulama (live_runner/paper/signal_runner
cbdr_width geçişi) AYRI-adım — Signal şemasına alan-ekleme gerektiriyor (N2 #23 şema-testi
sabit-set; genişleme ayrı-onay). Bu-commit: saf-fonksiyon + sözleşme-testi.

**Validation:** test_cbdr_multiplier 11/11 PASS; risk-tüketicileri 36+1skip PASS;
ruff PASS (mypy venv'de yok — hook-yolunda koşuyor).
